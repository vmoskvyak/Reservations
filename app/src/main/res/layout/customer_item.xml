<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.vmoskvyak.reservations.ui.fragments.customers.CustomerItemViewModel" />

        <variable
            name="click"
            type="com.vmoskvyak.reservations.ui.adapters.CustomersAdapter.OnCustomerClickListener" />
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{()->click.onCustomerClick(viewModel)}"
        card_view:cardBackgroundColor="@android:color/white"
        card_view:cardUseCompatPadding="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp">

            <ImageView
                android:id="@+id/iv_avatafr"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/account_circle"
                tools:ignore="ContentDescription" />

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginStart="18dp"
                android:layout_marginEnd="8dp"
                android:text="@{viewModel.getName()}"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="Martin Luter" />

        </LinearLayout>
    </android.support.v7.widget.CardView>

</layout>
